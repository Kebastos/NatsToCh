syntax = "proto3";

package message;

message NatsMsg {
  string id = 1;
  string client_id = 2;
  string subject = 3;
  string content = 4;
  // if we have to use tracing
  map<string, string> header = 5;
}